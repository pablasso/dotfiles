# ~/.tmuxinator/screeps.yml

project_name: screeps
project_root: ~/Development
windows:
  - screeps:
      root: ~/Development/screeps
      layout: main-vertical
      panes:
        - nvim .
        - nvm use 5.5 && gst
        - nvm use 5.5
