# ~/.tmuxinator/wizeline.yml

project_name: wizeline
project_root: ~/dev/wizeline
windows:
  - main:
      root: ~/dev/wizeline
      layout: main-vertical
      panes:
        - dockerize
        - dockerize
  - hubble:
      root: ~/dev/wizeline/hubble
      layout: main-vertical
      panes:
        - dockerize && workon hubble && gst
        - dockerize && nvim
  - montgomery:
      root: ~/dev/wizeline/montgomery
      layout: main-vertical
      panes:
        - dockerize && workon montgomery && gst
        - dockerize && nvim
  - assemble:
      root: ~/dev/wizeline/assemble
      layout: main-vertical
      panes:
        - dockerize && workon assemble && gst
        - dockerize && nvim
  - cerberus:
      root: ~/dev/wizeline/cerberus
      layout: main-vertical
      panes:
        - dockerize && workon cerberus && gst
        - dockerize && nvim
  - argonath:
      root: ~/dev/wizeline/argonath
      layout: main-vertical
      panes:
        - dockerize && workon argonath && gst
        - dockerize && nvim
