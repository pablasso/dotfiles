# ~/.tmuxinator/webrtc.yml

project_name: webrtc
project_root: ~/Development/oscar-ios
tabs:
  - iosapp:
      - gst
  - node-server:
      - cd ../oscar-node && gst && sudo node server.js
  - webapp:
      - cd ../oscar-web && gst && rails s
