# ~/.tmuxinator/webrtc.yml

project_name: webrtc
project_root: ~/Development/tokbox-iosapp
tabs:
  - iosapp:
      - gst
  - node-server:
      - cd ../tokbox-node-server && gst && node server.js
  - webapp:
      - cd ../tokbox-webapp && gst && rails s
