# ~/.tmuxinator/breezi-sync.yml

project_name: Breezi Sync 
project_root: ~/Development/breezi-sync
tabs:
  - console:
      layout: main-horizontal
      panes:
        -
  - misc:
      layout: main-horizontal
      panes:
        - bundle exec foreman start -c pubsub=1
        - bundle exec foreman start -c redis=1,mongodb=1
  - webworker: 
      layout: main-horizontal
      panes: 
        - bundle exec foreman start -c web=1
        - bundle exec foreman start -c worker=4
