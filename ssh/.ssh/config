Host *
    ServerAliveInterval 120
    ServerAliveCountMax 3
    IgnoreUnknown AddKeysToAgent,UseKeychain
    UseKeychain yes

# optional config I will not commit to the repo
Include ~/.ssh/personal.conf
